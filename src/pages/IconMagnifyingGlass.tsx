import { FunctionComponent } from "react";
import styles from "./IconMagnifyingGlass.module.css";

const IconMagnifyingGlass: FunctionComponent = () => {
  return (
    <div className={styles.iconMagnifyingGlass}>
      <img className={styles.vectorIcon} alt="" src="../vector3.svg" />
    </div>
  );
};

export default IconMagnifyingGlass;
